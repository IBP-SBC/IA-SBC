# IA-SBC · dependencias
# Las que ya están probadas en producción (sbc_ventas) van FIJAS.
# Las nuevas van con rango para no quedar atadas a un parche, pero
# acotadas a la versión mayor: un salto de mayor es donde rompen.
#
# NOTA: no usamos el SDK 'anthropic'. Se habla con la API por requests
# (ver nucleo/claude.py y el porqué que está documentado ahí).

streamlit==1.57.0
starlette==1.3.1          # NO subir a 1.4.0: rompe Streamlit en la nube
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0           # guarda el índice de fragmentos en parquet
requests==2.32.3          # Supabase Storage y API de Claude

# Lectura de documentos
pypdf>=5.1,<7             # PDF
python-docx>=1.1,<2       # Word
python-pptx>=1.0,<2       # PowerPoint
openpyxl==3.1.5           # Excel (xlsx/xlsm)
